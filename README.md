rally-story-pie-by-field
========================

Set a field name in the code and this will present a pie chart of the story points split across that field (generally a drop-down).  Also has a release selector box.